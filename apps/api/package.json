{
  "name": "@srl/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "prisma generate && tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "lint": "eslint src --ext .ts",
    "test": "DATABASE_URL=${TEST_DATABASE_URL:-mysql://root:root@127.0.0.1:3307/srlcanvas_test} prisma migrate deploy && vitest run",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma migrate deploy"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@prisma/client": "^6.19.3",
    "fastify": "^5.6.1",
    "fastify-plugin": "^6.0.0",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "jiti": "^1.21.7",
    "prisma": "^6.19.3",
    "tsx": "^4.20.6",
    "typescript": "^5.7.3",
    "vitest": "^4.0.18"
  }
}
